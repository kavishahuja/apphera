<p id="notice"><%= notice %></p>
<h3><%= I18n.t("keywords.show.google_ergebnisse") %> </h3>

<p>
<h4> Suchbegriff:
  <% begin %>
  <%= @keyword.keyword %> </h4>
</p>
<!-- <%= form_tag keywords_path, :action => :show, :method => :get do %>
    <%= select("keyword", "id", @keywords.collect { |r| [r.name, r.id] }) %>
    <%= submit_tag I18n.t("keywords.show.anzeigen_class_=>_btn") %>
<% end %>  -->

    <table class="table table-striped table-bordered table-condensed">
      <tr>
        <th><%= I18n.t("keywords.show.rank") %></th>
        <th><%= I18n.t("keywords.show.titel") %></th>
        <th><%= I18n.t("keywords.show.url") %></th>
      </tr>

      <% @keyword.results.each do |ranking| %>
          <tr>
            <% if ranking.link.include? @url %>
            <td><h5><%= ranking.ranking %> **</h5></td>
                <% else %>
                <td><h5><%= ranking.ranking %></h5></td>

          <% end %>
            <td><h5><%= ranking.link_text %></h5></td>


            <td><a href="<%= ranking.link %>" target="_blank"><%= ranking.link[0..30] + "..." || str %></a></td>

          </tr>
      <% end %>
    </table>
      <% rescue %>
      <% end %>
    <br/>
<%= link_to I18n.t("form_back"), keywords_path %>

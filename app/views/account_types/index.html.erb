<h1><%= I18n.t("account_types.index.listing_account_types") %></h1>

<table>
  <tr>
    <th><%= I18n.t("account_types.index.name") %></th>
    <th><%= I18n.t("account_types.index.description") %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @account_types.each do |account_type| %>
  <tr>
    <td><%= account_type.name %></td>
    <td><%= account_type.description %></td>
    <td><%= link_to I18n.t("account_types.index.show"), account_type %></td>
    <td><%= link_to I18n.t("account_types.index.edit"), edit_account_type_path(account_type) %></td>
    <td><%= link_to I18n.t("account_types.index.destroy"), account_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to I18n.t("account_types.index.new_account_type"), new_account_type_path %>
